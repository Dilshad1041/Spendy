import 'package:flutter/material.dart';

class Themes {
  static ThemeData primaryTheme = ThemeData(
      primarySwatch: Colors.green,
      appBarTheme: const AppBarTheme(color: Color.fromARGB(255, 0, 24, 1)),
      scaffoldBackgroundColor: Colors.white);
}
